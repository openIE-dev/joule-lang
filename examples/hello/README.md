# hello

The minimum Joule program.

```bash
joulec build hello.joule -o hello
./hello
```

What this shows:
- `fn main() -> i32:` declaration with indentation-style block
- `println(...)` builtin
- Explicit `return 0` for exit code
